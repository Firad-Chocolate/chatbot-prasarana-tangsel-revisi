rest:
  # Channel REST API standar untuk testing via HTTP request

socketio:
  user_message_evt: user_uttered
  bot_message_evt: bot_uttered
  session_persistence: true
  cors_allowed_origins:
    - "https://chatbot-prasarana-tangsel.netlify.app"


#socketio:
#  user_message_evt: user_uttered
#  bot_message_evt: bot_uttered
#  session_persistence: true
#  user_output_room: "session_id"

# Contoh integrasi Telegram (opsional, isi token Anda sendiri):
# telegram:
#   access_token: "YOUR_TELEGRAM_TOKEN"
#   verify: "YOUR_BOT_USERNAME"
#   webhook_url: "YOUR_WEBHOOK_URL"